{
  "private": true,
  "scripts": {
    "format": "prettier --write specs playwright.config.mjs package.json compose.yaml keycloak/realm.json",
    "format:check": "prettier --check specs playwright.config.mjs package.json compose.yaml keycloak/realm.json",
    "lint": "oxlint specs playwright.config.mjs",
    "prepare": "git config core.hooksPath .githooks",
    "test": "playwright test"
  },
  "lint-staged": {
    "*.cs": "dotnet csharpier format",
    "*.{html,json,md,mjs,yaml,yml}": "prettier --write"
  },
  "devDependencies": {
    "@playwright/test": "1.55.0",
    "lint-staged": "^17.5.1",
    "oxlint": "^1.81.0",
    "prettier": "^3.9.6"
  }
}
